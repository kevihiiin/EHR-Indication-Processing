[project]
name = "ehr-indication-deployment"
version = "0.1.0"
description = "Deployment dependencies for the EHR indication classifier"
requires-python = ">=3.10"
dependencies = [
    "datasets==3.0.1",
    "pandas==2.2.3",
    "tqdm==4.66.5",
    "torch==2.4.1",
    "transformers==4.45.2",
]
